# Astro page routes (src/pages/**/*.astro) have no unit-test convention in
# this repo — they need getStaticPaths/Astro.site/GraphQL-fetch mocking that
# no test here currently does. Excluded from coverage instead of flagging
# every new or touched page route.
ignore:
  - "src/pages/**/*.astro"
